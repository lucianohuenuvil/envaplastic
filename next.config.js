/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    theme: {
        fontFamily: {
            sans: ['sans-serif']
        }
    },
}

module.exports = nextConfig
